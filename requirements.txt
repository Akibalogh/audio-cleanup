# Core
librosa>=0.10
numpy
soundfile

# Default detector (--detector panns) and song splitting
panns-inference
torch>=2.0

# Optional: --method stem
demucs>=4.0

# Song titling from lyrics (--title-songs); also needs ffmpeg on PATH
openai-whisper
